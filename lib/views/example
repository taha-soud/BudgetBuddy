Purpose: Contains the UI components, such as screens and widgets.
Contents: Dart files for your screens and any specific widgets associated with them (e.g., home_screen.dart, login_screen.dart).


##example
import 'package:flutter/material.dart';
import '../view_models/user_view_model.dart';
import 'package:provider/provider.dart';

class HomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ChangeNotifierProvider(
      create: (context) => UserViewModel(User(id: '1', name: 'John Doe')),
      child: Scaffold(
        appBar: AppBar(title: Text('Home')),
        body: Consumer<UserViewModel>(
          builder: (context, viewModel, child) => Text('Welcome ${viewModel.user.name}'),
        ),
      ),
    );
  }
}
